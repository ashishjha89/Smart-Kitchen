<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:layout_marginBottom="16dp"
    android:layout_marginRight="16dp">

    <ImageView
        android:id="@+id/recipe_icon"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:scaleType="centerCrop"
        tools:src="@drawable/ic_home_black_24dp" />

    <TextView
        android:id="@+id/recipe_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="#2C3D4E"
        android:textSize="14sp"
        app:layout_constraintLeft_toLeftOf="@+id/recipe_icon"
        app:layout_constraintRight_toRightOf="@+id/recipe_icon"
        app:layout_constraintTop_toBottomOf="@+id/recipe_icon"
        tools:text="Great food" />

</android.support.constraint.ConstraintLayout>